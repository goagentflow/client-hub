# Server
NODE_ENV=development
PORT=3001

# Auth mode: azure_ad (real JWT) or demo (X-Dev-User-Email)
AUTH_MODE=demo

# Data backend: azure_pg (PostgreSQL via Prisma)
DATA_BACKEND=azure_pg

# PostgreSQL connection string (required)
DATABASE_URL=postgresql://user:password@localhost:5432/agentflow

# Azure AD app registration (required by env validation)
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
# Optional today (used for OBO/Graph flows when enabled)
# AZURE_CLIENT_SECRET=your-client-secret

# Optional JWT overrides
# AZURE_JWKS_URI=https://login.microsoftonline.com/<tenant>/discovery/v2.0/keys
# STAFF_ROLE_NAME=Staff

# Portal auth
# Must be at least 32 chars in production
PORTAL_TOKEN_SECRET=dev-portal-secret-change-in-production-min-32-chars
# Optional: JSON map of static pitch slug -> legacy simple hash
# Example: {"dcm-ac17d8":"5e918d2","kao-7f3x9k":"51767b91"}
PITCH_PASSWORD_HASH_MAP={}

# Proxy trust (set true behind Cloud Run / reverse proxy)
TRUST_PROXY=false

# CORS
CORS_ORIGIN=http://localhost:5173

# Email (Resend) - optional for local dev, required for production email flows
# RESEND_API_KEY=re_xxx
RESEND_FROM_EMAIL=noreply@goagentflow.com

# Supabase Storage - required for real document upload/download
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Logging
LOG_LEVEL=debug
