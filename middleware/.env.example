# Server
NODE_ENV=development
PORT=3001

# Auth mode: azure_ad (real JWT) or demo (X-Dev-User-Email header)
AUTH_MODE=demo

# Data backend: azure_pg (PostgreSQL via Prisma) or mock (in-memory)
DATA_BACKEND=mock

# PostgreSQL (required when DATA_BACKEND=azure_pg)
# DATABASE_URL=postgresql://user:password@host:5432/agentflow?sslmode=require

# Azure AD - App Registration
# Get these from Azure Portal > App Registrations
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
# AZURE_CLIENT_SECRET=your-client-secret  # Only needed for Phase 4 (OBO)

# CORS - frontend URL
CORS_ORIGIN=http://localhost:5173

# Logging
LOG_LEVEL=debug
